# Add environment variables only after your project's configuration is specified.
# Document each variable's purpose, requiredness, and safe example value here.
# Never put real credentials in this file. Local .env files are gitignored.
